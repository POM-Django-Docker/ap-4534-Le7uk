
# Django_Docker

To run docker-compose, you should create a `.env` file in the root directory with the following values:

```env
DB_NAME=DB_NAME
DB_USER=DB_USER
DB_PASSWORD=DB_PASSWORD
DB_HOST=db
DB_PORT=DB_PORT
```
